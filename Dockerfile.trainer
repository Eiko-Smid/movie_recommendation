FROM alpine:3.20

# Install required tools
RUN apk add --no-cache \
    curl \
    bash \
    busybox-suid

# Create log directory
RUN mkdir -p /var/log

CMD ["crond", "-f", "-l", "2"]

# Copy optional scripts (if needed later)
WORKDIR /app